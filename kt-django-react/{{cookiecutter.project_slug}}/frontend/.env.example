# Frontend environment variables (Vite)
# Copy to .env.local for local overrides — Vite automatically reads .env.local

# Backend API base URL (default: /api, proxied by Vite dev server)
VITE_API_BASE=http://localhost:8000/api